1.INNER JOIN 
	- Syntax:
	
	SELECT COLUMN_NAME FROM TABLE_1
	INNER JOIN TABLE_2
	ON TABLE_1 . COLUMN_NAME = TABLE_2 . COULMN_NAME;
			|			|
		   FOREIGN KEY		   PRIMARY KEY

	
2.CREATE TABLE WITH FOREIGN KEY SYNTAX : 

	CREATE TABLE STUDENT(
		ID INT NOT NULL AUTO_INCREMENT,
		NAME VARCHAR(30) NOT NULL,
		AGE INT NOT NULL,
		CITY VARCHAR(10) NOT NULL,
		PRIMARY KEY (ID),
		FOREIGN KEY(CITY) REFERANCES CITY(CID)
	
	);


3.COMMAND FOR SETING FOREIGN KEY FOR EXISTING TABLE : 
	
	ALTER TABLE TABLE_NAME
	ADD FOREIGN KEY(CITY) REFERENCES CITY(CID);	